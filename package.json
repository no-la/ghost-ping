{
  "name": "ghost-ping",
  "version": "0.1.0",
  "description": "Obsidian plugin: notify when uncreated notes receive new backlinks over a threshold",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "builtin-modules": "^5.2.0",
    "esbuild": "0.28.1",
    "obsidian": "latest",
    "tslib": "2.4.0",
    "typescript": "4.7.4",
    "vitest": "^4.1.7"
  }
}
